# ─── Database Credentials ───
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=kl_ciie
DB_PORT=5432

# ─── Redis ───
REDIS_PORT=6379

# ─── JWT (CHANGE THIS in production!) ───
JWT_SECRET=change-this-to-a-secure-random-string-at-least-16-chars

# ─── API Server ───
API_PORT=3001

# ─── CORS (comma-separated origins) ───
CORS_ORIGIN=http://localhost,http://localhost:80,https://your-domain.com

# ─── SMTP (optional, fallback if DB smtp_settings empty) ───
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM="KL CIIE <your-email@gmail.com>"
